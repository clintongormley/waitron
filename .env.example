# ─── Database ────────────────────────────────────────────────────────────────
# PostgreSQL connection URL
DATABASE_URL=postgresql://waitron:changeme@localhost:5432/waitron

# Individual Postgres config (used by docker-compose)
POSTGRES_USER=waitron
POSTGRES_PASSWORD=changeme
POSTGRES_DB=waitron
POSTGRES_PORT=5432

# ─── Redis ───────────────────────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379
REDIS_PORT=6379

# ─── Auth ────────────────────────────────────────────────────────────────────
# !! Change this to a long random secret in production !!
# Generate with: openssl rand -hex 64
JWT_SECRET=replace-me-with-a-long-random-secret
JWT_EXPIRY=7d

# ─── API ─────────────────────────────────────────────────────────────────────
# Port the NestJS API listens on
API_PORT=3001
NODE_ENV=production

# Platform mode: self-hosted | saas
PLATFORM_MODE=self-hosted

# ─── Frontend ────────────────────────────────────────────────────────────────
# URL the browser uses to reach the API (must be publicly accessible)
NEXT_PUBLIC_API_URL=http://localhost:3001

# ─── Payments ────────────────────────────────────────────────────────────────
# Which payment provider to use: stripe | square | mock
PAYMENT_PROVIDER=mock

# Stripe (if PAYMENT_PROVIDER=stripe)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Square (if PAYMENT_PROVIDER=square)
# SQUARE_ACCESS_TOKEN=...
# SQUARE_LOCATION_ID=...
# SQUARE_ENVIRONMENT=sandbox
