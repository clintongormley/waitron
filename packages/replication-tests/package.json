{
  "name": "@waitron/replication-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "@waitron/db": "workspace:*",
    "@waitron/fiscal-verifactu": "workspace:*",
    "@waitron/provisioning": "workspace:*",
    "@waitron/shared": "workspace:*",
    "@waitron/sync": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
