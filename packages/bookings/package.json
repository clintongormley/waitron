{
  "name": "@waitron/bookings",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./dashboard": "./src/dashboard/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "db:generate": "drizzle-kit generate",
    "db:generate:custom": "drizzle-kit generate --custom"
  },
  "dependencies": {
    "@waitron/dashboard-kit": "workspace:*",
    "@waitron/db": "workspace:*",
    "@waitron/identity": "workspace:*",
    "@waitron/module": "workspace:*",
    "@waitron/server-kit": "workspace:*",
    "@waitron/shared": "workspace:*",
    "@waitron/sync-enrolment": "workspace:*",
    "@waitron/ui": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.6.0",
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "@types/node": "^24.0.0",
    "@vitest/browser": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "@waitron/migrations": "workspace:*",
    "drizzle-kit": "^0.31.10",
    "playwright": "^1.49.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
