-- The recipient column on the immutable registro de facturación, for F3 canje (piece 3 of the
-- fiscal sequence). docs/superpowers/plans/2026-08-02-f3-canje.md §2.2.
--
-- `destinatarios` stores RegistroAlta["Destinatarios"] — NULL on an ordinary F2 alta and on an
-- anulación, set on a full invoice (F3, and later F1). `jsonb`, not `text`, is safe because it is
-- NOT a huella input (huella.ts hashes 8 named fields, the recipient is not among them), so it is
-- only ever re-serialised into XML, never digested — identical to `desglose`/`facturas_*`. A field
-- that WERE hashed would need `text` for byte-identity, like `cuota_total`/`importe_total`.
--
--   registros_facturas_sustituidas_f3_ck — defense-in-depth (mirrors
--     registros_tipo_factura_rectificativa_ck): a FacturasSustituidas block may only sit on an F3.
--     `facturas_sustituidas` itself was added by 0010 (unpopulated by the rectificativa work); this
--     migration adds only the constraint. The `tipo_factura is not null` arm closes the same
--     three-valued-logic hole Copilot found on #46 (`NULL = 'F3'` is NULL, which a CHECK passes).
--
-- Immutability and RLS carry automatically: 0001_registros_inmutables.sql's REVOKE/GRANT, the
-- row/statement reject_mutation triggers and the tenant-isolation policy are all TABLE-LEVEL,
-- covering any column with no privilege/trigger/policy DDL here. NULLABLE with no backfill
-- (pre-production, no deployed data). Generated by plain `drizzle-kit generate` against
-- ./src/schema/registros.ts, keeping drizzle/meta/0011_snapshot.json accurate.
ALTER TABLE "registros_facturacion" ADD COLUMN "destinatarios" jsonb;--> statement-breakpoint
ALTER TABLE "registros_facturacion" ADD CONSTRAINT "registros_facturas_sustituidas_f3_ck" CHECK ("registros_facturacion"."facturas_sustituidas" is null or ("registros_facturacion"."tipo_factura" is not null and "registros_facturacion"."tipo_factura" = 'F3'));