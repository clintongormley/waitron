# Test-load implementation and verification

1. Compare unfinished packages and files across the supplied CI logs. Reproduce locally with
   cold browser caches and the original four-package concurrency, retaining debug logs.
2. Add failing guards for CI partitioning, concurrency, deadlines and Bookings project order.
   Add real child-process tests for timeout, blocked event loops, cancellation and descendants.
3. Apply the configuration changes and local deadline wrapper described in the
   [design](../specs/2026-09-09-test-load-design.md). Keep coverage assertions and scope guards.
4. Run Bookings coverage repeatedly, check that browser files follow the Node files, and run
   Sync coverage. Prove the outer timeout with a blocked worker and a real stalled browser.
5. Capture the reproduced local migration stall with PostgreSQL and Node state. Reduce it to
   bounded queries of different sizes; verify the network correction on the same container.
   Add the failing network-interface and peer-name regressions, fix every fixture consumer,
   and repeat the fiscal/Sync coverage selection three times.
6. Run the root coverage suite, package typechecks and the whole repository gate. Record
   measured outcomes, update the working notes and backlog, and state any failure not reproduced.
