{
  "name": "@waitron/dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@simplewebauthn/browser": "^13.0.0",
    "@waitron/catalogue": "workspace:^",
    "@waitron/dashboard-kit": "workspace:*",
    "@waitron/dashboard-modules": "workspace:*",
    "@waitron/diagnostics": "workspace:*",
    "@waitron/printing": "workspace:*",
    "@waitron/shared": "workspace:*",
    "@waitron/ui": "workspace:*",
    "lit": "^3.2.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "@vitest/browser-playwright": "^4.1.11",
    "@vitest/coverage-v8": "^4.1.11",
    "@waitron/layouts": "workspace:*",
    "axe-core": "^4.13.0",
    "playwright": "^1.63.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.11"
  }
}
